# Build this plugin: `mise run build`
[tasks.build]
description = "Fetch+componentize pandoc, cross-build pdfTeX + texmf, then build + tag the bash and doctools images"
dir = "{{config_root}}"
run = "./build.sh"
