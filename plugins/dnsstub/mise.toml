# Build this plugin: `mise run build`
[tasks.build]
description = "Build the hermetic DNS stub server node (authoritative for wk.test)"
dir = "{{config_root}}"
run = "WASI_SDK=$(mise where github:WebAssembly/wasi-sdk) ./build.sh"
