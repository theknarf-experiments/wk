[package]
name = "sequencer"
version = "0.1.0"
edition = "2024"

[dependencies]
# The music itself: the song model, the transport arithmetic and the MIDI-file
# conversion live in workspace crates so they can be tested where tests run.
# What is left here is the window: drawing, clicks, and talking to wk.
wk-sequence = { path = "../../crates/wk-sequence" }
wk-midifile = { path = "../../crates/wk-midifile" }
wk-pixelfont = { path = "../../crates/wk-pixelfont" }
wit-bindgen-rt = { version = "0.44.0", features = ["bitflags"] }

[lib]
crate-type = ["cdylib"]

[package.metadata.component]
package = "wk:sequencer"

[package.metadata.component.dependencies]

[package.metadata.component.target.dependencies]
"wasi:surface" = { path = "wit/deps/wasi-surface" }
"wasi:graphics-context" = { path = "wit/deps/wasi-graphics-context" }
"wasi:frame-buffer" = { path = "wit/deps/wasi-frame-buffer" }
"wasi:io" = { path = "wit/deps/wasi-io" }
"wk:midi" = { path = "wit/deps/wk-midi" }
"wk:options" = { path = "wit/deps/wk-options" }
