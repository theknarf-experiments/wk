=== FRONTIER (accurate): 100% of bun C++/Rust/JSC links; ONLY vendored C libs remain ===

Every bun C++ binding + generated .cpp + the modules blob compiles, and the
trial link (plugins/bun/link/link.sh) resolves ALL of JSC/WTF/ICU/Rust/
bindings. Correction to earlier notes: LLD's default 20-error cap HID the
true scale. With -Wl,--error-limit=0 the real count is 1287 UNIQUE undefined
symbols, and they are ALL from vendored C libraries that bun's Rust build.rs
did NOT cross-compile for wasi (they were cfg'd out / use non-wasm build
systems). Full list: plugins/bun/link/undefined-symbols.txt.

VENDORED C LIBS TO CROSS-COMPILE FOR wasi (unique-symbol counts):
  530  BoringSSL (crypto)  — the big one. CMake; use the C-only no-asm path
       (OPENSSL_NO_ASM). Headers in native/boringssl. Referenced by node:crypto/
       tls/X509. This is most of the work.
  333  uSockets            — packages/bun-usockets. HARDEST: needs a wasi
       event-loop backend (no epoll/kqueue). Tie to [[wk-userspace-network]]
       smoltcp, or a poll(2)/stub backend. All the us_*/uws_* symbols.
   37  libarchive          — native/libarchive. Standard C cross-build.
   29  c-ares (DNS)        — native/cares. Standard C.
   25  zstd                — native/zstd (VERIFIED cross-compiles w/
       -DZSTD_DISABLE_ASM). Standard C.
   16  brotli              — native/brotli/c. Standard C.
   15  simdutf             — the SIMD unicode lib (bun's simdutf_sys didn't
       compile its C++ for wasi). Has a scalar fallback path.
   14  zlib(-ng)           — native/zlib. Standard C.
    3  Bun::Secrets::*     — OS keychain; no wasi equiv. STUB (return error).
    +  WebP (image), a few singles.

APPROACH: each standard-C lib (zstd/zlib/brotli/cares/libarchive/simdutf)
cross-compiles cleanly with wasi-sdk clang -O2 (+ its -DNO_ASM flags) — build
all its .c into a .a, add to link.sh. BoringSSL is the large mechanical one.
uSockets needs the event-backend design. THEN the component links + should
run console.log (crypto/net stubbed inert until their libs land).

Also 13 C++<->Rust ABI sig-mismatch WARNINGS remain (i64/i32 JSVALUE32_64;
don't block link but crash at runtime — fix for a WORKING run; listed above
in git history / earlier punchlist).

DONE & REUSABLE: cxx-flags.rsp (-fno-rtti!), link.sh, wasi-stubs.c,
InternalModuleRegistryConstants.wasm.S, gen-codegen.ts (all codegen).
