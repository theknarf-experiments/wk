=== C++ bindings: ~496/520 non-v8 compile (95%); +42 v8 shim excluded ===
=== Remaining ~21, in 3 buckets by root cause ===

--- BUCKET A: JSC features disabled in our JSCONLY cloop build ---
Our native/jsc-build/cmakeconfig.h has: ENABLE_WEBASSEMBLY 0,
ENABLE_SAMPLING_PROFILER 0, ENABLE_REMOTE_INSPECTOR 0, ENABLE_JIT 0.
These genuinely can't work on wasm (JIT impossible, inspector needs
sockets, profiler needs signals, wasm-in-wasm exotic). Gate the bun C++
with #if ENABLE(...) + stub the Rust-facing extern "C" exports. NOTE:
must be a COORDINATED cross-language disable — bun_runtime (Rust) also has
wasm-streaming/inspector paths that call these symbols; gating only the
C++ creates link-time undefined symbols. Do Rust + C++ together per feature.
  WEBASSEMBLY:  ZigGlobalObject.cpp (core; wasm block at lines 150-151,
                2359-2361, 3996-4058), JSWasmStreamingCompiler.cpp, NodeVM.cpp
                (createJSWebAssemblyCompileError)
  SAMPLING_PROFILER: BunCPUProfiler.cpp (whole file), InternalModuleRegistry.cpp
  REMOTE_INSPECTOR:  BunDebugger.cpp, ConsoleObject.cpp (core),
                InspectorBunFrontendDevServerAgent/HTTPServerAgent/LifecycleAgent.cpp
  JIT:          NodeVMScript.cpp, NodeVMSourceTextModule.cpp

--- BUCKET B: 32-bit-pointer ABI (need real wasm32 variants, like JSVALUE32_64) ---
These static_assert 64-bit pointers or pack bits into a 64-bit pointer.
Must stay ABI-consistent with the Rust side.
  StrongRef.cpp — packs 48-bit slot ptr + 16-bit index into one pointer.
    HARD: needs a 2-word StrongRefImpl on 32-bit (Rust Strong.rs SLOT_MASK
    fix already done but the C++ encode/decode must agree). Core mechanism.
  BunProcess.cpp, JSBuffer.cpp, napi.cpp, JSONRowsToJS.cpp,
  StringBuilderBinding.cpp — sizeof static_asserts assuming 8-byte layout.

--- BUCKET C: version-drift / misc ---
  NodeSqlite.cpp — JSC::DestructionMode / NeedsDestruction absent from our
    JSC (a USE_BUN_JSC_ADDITIONS type not exported). sqlite; optional for bun run.
  TopExceptionScopeBinding.cpp — JSC::TopExceptionScope missing (version drift).
  SerializedScriptValue.cpp — narrowing conversion (wasm32 size_t).

=== ALREADY FIXED this session (were failing, now compile) ===
lut.h namespace (create-hash-table.ts), JSCompressionStream using-decls,
napi.h <vector>, usockets __wasi__ mutex, termios/spawn/epoll/pwd/signal
shims (wasi-compat/), CPUFeatures wasm arm, c-bindings tty/socket gates,
EventContext/EventPath/WorkerMessagingProxy/CryptoMLDSA/MLKEM includes,
JSBundlerPlugin/JSFFIFunction pointer<->double bit_cast, JSMockFunction/
JSCommonJSModule void-lambda RETURN_IF_EXCEPTION.
