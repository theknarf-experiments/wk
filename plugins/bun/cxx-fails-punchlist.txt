=== C++ bindings: ALL 520 non-v8 files compile for wasm32-wasip2 (0 fails) ===

The entire hand-written + optional C++ bindings layer builds. StrongRef's
32-bit GC-handle redesign (the last core blocker) is done; the 6 optional
subsystems (inspector/profiler/wasm-streaming/sqlite) are gated to their
ENABLE flags with linkable no-op stubs for Rust-called exports.

=== REMAINING to a linked `bun run` component ===
1. GENERATED .cpp — compile GeneratedBindings.cpp, WebCoreJSBuiltins.cpp,
   JSSink.cpp, ZigGeneratedClasses.cpp, ErrorCode.cpp, etc. (codegen/*.cpp
   from bindgen.ts/bundle-functions.ts/generate-classes.ts) with the same
   cxx-flags.rsp.
2. v8 SHIM (42 files, src/jsc/bindings/v8/**) — real V8 headers reject
   wasm32 ("Host architecture not supported"). Native addons can't load on
   wasm, so EXCLUDE from the link and stub whatever napi/runtime symbols the
   linker reports undefined.
3. VENDORED C LIBS — cross-compile to .a for wasi: boringssl, zlib(-ng),
   lshpack, lolhtml, cares, brotli, zstd, libarchive, picohttpparser,
   bun-usockets, mimalloc, tinycc(?). Headers already fetched in native/.
4. LINK: libbun_rust.a + all bindings .o + generated .o + vendored .a +
   {JSC,WTF,bmalloc,ICU,c++,c++abi,setjmp,wasi-emulated-*} + -z stack-size=8M
   via wasm-component-ld → the bun-run component.

Recipe: plugins/bun/cxx-flags.rsp (clang response file, $BUN_NATIVE/$BUN_CODEGEN
/$BUN_PLUGIN placeholders). All codegen headers reproduced via gen-codegen.ts.
