--- a/bridges/CustomTarget_gcc3_wasm.mk
+++ b/bridges/CustomTarget_gcc3_wasm.mk
@@ -13,10 +13,12 @@
     generated-cxx.cxx \
     generated-asm.s \
     exports \
+    generated-rtti.cxx \
 ))
 
 $(gb_CustomTarget_workdir)/bridges/gcc3_wasm/generated-asm.s \
 $(gb_CustomTarget_workdir)/bridges/gcc3_wasm/generated-cxx.cxx \
+$(gb_CustomTarget_workdir)/bridges/gcc3_wasm/generated-rtti.cxx \
 $(gb_CustomTarget_workdir)/bridges/gcc3_wasm/exports: \
         $(call gb_Executable_get_target_for_build,wasmbridgegen) \
         $(call gb_UnoApi_get_target,udkapi) \
@@ -25,6 +27,7 @@
         $(gb_CustomTarget_workdir)/bridges/gcc3_wasm/generated-cxx.cxx \
         $(gb_CustomTarget_workdir)/bridges/gcc3_wasm/generated-asm.s \
         $(gb_CustomTarget_workdir)/bridges/gcc3_wasm/exports \
+        $(gb_CustomTarget_workdir)/bridges/gcc3_wasm/generated-rtti.cxx \
         +$(call gb_UnoApi_get_target,udkapi) +$(call gb_UnoApi_get_target,offapi)
 
 # vim: set noet sw=4 ts=4:
--- a/bridges/Library_cpp_uno.mk
+++ b/bridges/Library_cpp_uno.mk
@@ -94,6 +94,7 @@
 ))
 $(eval $(call gb_Library_add_generated_exception_objects,$(CPPU_ENV)_uno, \
     CustomTarget/bridges/gcc3_wasm/generated-cxx \
+    $(if $(filter WASI,$(OS)),CustomTarget/bridges/gcc3_wasm/generated-rtti) \
 ))
 $(eval $(call gb_Library_use_static_libraries,$(CPPU_ENV)_uno, \
     emscriptencxxabi \
--- a/bridges/source/cpp_uno/gcc3_wasm/cpp2uno.cxx
+++ b/bridges/source/cpp_uno/gcc3_wasm/cpp2uno.cxx
@@ -12,7 +12,9 @@
 #include <cstring>
 #include <typeinfo>
 
+#if defined EMSCRIPTEN
 #include <emscripten.h>
+#endif
 
 #include <com/sun/star/uno/RuntimeException.hpp>
 #include <o3tl/string_view.hxx>
@@ -29,6 +31,8 @@
 
 #include "abi.hxx"
 
+#if defined EMSCRIPTEN
+
 EM_JS(void*, jsGetExportedSymbol, (char const* name),
       // clang-format off
 {
@@ -37,6 +41,32 @@
 }
       // clang-format on
 );
+
+#else
+
+// No JS runtime to ask, so the answer is decided at link time instead:
+// wasmbridgegen emits generated-rtti.cxx, a table of every UNO exception type's
+// _ZTI symbol taken as a WEAK reference. Weak means an entry for a type this
+// binary does not link resolves to null rather than failing the link, which is
+// the same contract jsGetExportedSymbol has — so the fallback below stays
+// reachable, unchanged, for exactly the types it was reachable for before.
+//
+// Do NOT be tempted to skip the table and let the fallback synthesize RTTI for
+// everything. It compiles, links, and silently does not work: wasi-sdk's
+// <typeinfo> selects the "unique" comparison implementation, which compares
+// type_info by ADDRESS, so a synthesized type_info with a byte-identical
+// mangled name matches nothing. Verified by running: a throw with synthesized
+// RTTI falls through catch (Derived&) AND catch (Base&) into catch (...),
+// while the same throw with the real RTTI is caught normally. Every
+// catch (css::uno::Exception&) in LibreOffice would quietly stop firing.
+extern "C" void* wasm_getRttiSymbol(char const* name);
+
+namespace
+{
+void* jsGetExportedSymbol(char const* name) { return wasm_getRttiSymbol(name); }
+}
+
+#endif
 
 using bridges::cpp_uno::shared::VtableFactory;
 
--- a/static/source/wasmbridgegen/wasmbridgegen.cxx
+++ b/static/source/wasmbridgegen/wasmbridgegen.cxx
@@ -49,7 +49,7 @@
 {
     std::cerr
         << "Usage:\n\n"
-           "  wasmbridgegen <cpp-output> <asm-output> <exp-output> <registries>\n\n"
+           "  wasmbridgegen <cpp-output> <asm-output> <exp-output> <rtti-output> <registries>\n\n"
            "where each <registry> is '+' (primary) or ':' (secondary), followed by: either a\n"
            "new- or legacy-format .rdb file, a single .idl file, or a root directory of an\n"
            ".idl file tree.  For all primary registries, Wasm UNO bridge code is written to\n"
@@ -667,15 +667,20 @@
     try
     {
         auto const args = rtl_getAppCommandArgCount();
-        if (args < 3)
+        if (args < 4)
         {
             badUsage();
         }
         auto const cppPathname = getPathnameArgument(0);
         auto const asmPathname = getPathnameArgument(1);
         auto const expPathname = getPathnameArgument(2);
+        // The fourth output is for platforms with no JS runtime to ask for a
+        // symbol's address: the same set of exception RTTI symbols the exports
+        // file lists, but as a table the bridge itself can search. See the
+        // comment on the writer below.
+        auto const rttiPathname = getPathnameArgument(3);
         rtl::Reference<TypeManager> mgr(new TypeManager);
-        for (sal_uInt32 i = 3; i != args; ++i)
+        for (sal_uInt32 i = 4; i != args; ++i)
         {
             auto const & [ uri, primary ] = parseRegistryArgument(i);
             try
@@ -1023,6 +1028,63 @@
         if (!expOut)
         {
             std::cerr << "Failed to write \"" << expPathname << "\"\n";
+            std::exit(EXIT_FAILURE);
+        }
+        // The RTTI table. Emscripten resolves an exception type's _ZTI symbol
+        // by asking the JS runtime for the module's export of that name; a
+        // platform without one has to be told at link time, which is what this
+        // is.
+        //
+        // Every entry is a WEAK reference, and that is the whole trick: a weak
+        // undefined symbol links to a null address instead of failing, and does
+        // not drag the archive member that would define it into the link. So
+        // the table costs only its own entries, and an entry for a type this
+        // binary never links resolves to null -- which is exactly the contract
+        // jsGetExportedSymbol has, so the bridge's existing fallback path stays
+        // reachable, unchanged, for those.
+        //
+        // Names carry the extra leading underscore Emscripten's export list
+        // uses; the linker symbol, and what the bridge looks up, is one
+        // underscore shorter.
+        std::ofstream rttiOut(rttiPathname, std::ios_base::out | std::ios_base::trunc);
+        if (!rttiOut)
+        {
+            std::cerr << "Cannot open \"" << rttiPathname << "\" for writing\n";
+            std::exit(EXIT_FAILURE);
+        }
+        rttiOut << "// Generated by wasmbridgegen. Do not edit.\n\n"
+                   "#include <sal/config.h>\n\n"
+                   "#include <cstring>\n\n"
+                   "extern \"C\" {\n\n";
+        for (auto const& rtti : rttis)
+        {
+            rttiOut << "extern char " << (rtti.getStr() + 1) << " __attribute__((weak));\n";
+        }
+        rttiOut << "\n}\n\nnamespace {\n\n"
+                   "struct Entry { char const * name; void * rtti; };\n\n"
+                   "// Sorted by name: wasmbridgegen collects these in a std::set,\n"
+                   "// so a binary search below is valid.\n"
+                   "Entry const entries[] = {\n";
+        for (auto const& rtti : rttis)
+        {
+            rttiOut << "    { \"" << (rtti.getStr() + 1) << "\", &" << (rtti.getStr() + 1)
+                    << " },\n";
+        }
+        rttiOut << "};\n\n}\n\n"
+                   "extern \"C\" void * wasm_getRttiSymbol(char const * name) {\n"
+                   "    std::size_t lo = 0, hi = sizeof entries / sizeof entries[0];\n"
+                   "    while (lo < hi) {\n"
+                   "        std::size_t const mid = lo + (hi - lo) / 2;\n"
+                   "        int const cmp = std::strcmp(name, entries[mid].name);\n"
+                   "        if (cmp == 0) { return entries[mid].rtti; }\n"
+                   "        if (cmp < 0) { hi = mid; } else { lo = mid + 1; }\n"
+                   "    }\n"
+                   "    return nullptr;\n"
+                   "}\n";
+        rttiOut.close();
+        if (!rttiOut)
+        {
+            std::cerr << "Failed to write \"" << rttiPathname << "\"\n";
             std::exit(EXIT_FAILURE);
         }
         return EXIT_SUCCESS;
