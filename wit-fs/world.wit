package wk:fshost;

/// The host side of wasi:filesystem: wk implements these so plugins get an
/// in-memory virtual filesystem.
world fs-host {
    import wasi:filesystem/types@0.2.12;
    import wasi:filesystem/preopens@0.2.12;
}
