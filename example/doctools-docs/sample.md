# Sample notes

Written in *markdown*, converted by the **real pandoc** (GHC wasm build)
running inside wk — try some math: $e^{i\pi} + 1 = 0$.

## A list

- pandoc turns this file into LaTeX (or HTML, rst, docx, ...)
- the conversion runs entirely inside the sandbox
- edit me in the vim node, then re-run pandoc in the shell

## A table

| tool   | source              | runs as        |
|--------|---------------------|----------------|
| pandoc | GHC wasm backend    | wasm component |
| bash   | GNU bash 5.2        | wasm component |
